\DTLgidxSetDefaultDB{notation}
% NOTE: It seems datagidx only wants items here to be those that are in the document. Please comment out unused notations. Also, please be careful with the commas and brackets.

\newterm
[%
label={not:set},
description={a collection of distinct objects},
sort={s}
]%
{ \ensuremath{\mathcal{S}} }

\newterm
[%
label={not:universalSet},
description={the set containing everything},
sort={u}
]%
{ \ensuremath{\mathcal{U}} }

\newterm
[%
label={not:emptySet},
description={the set with no elements},
sort={e}
]%
{ \ensuremath{\emptyset} }

\newterm
[%
label={not:cardinality},
description={the number of elements in the set \ensuremath{\mathcal{S}}},
sort={c}
]%
{ \ensuremath{|\mathcal{S}|} }

\newterm
[%
label={not:ir},% label
description={impulse response},%
sort={h}
]%
{ \ensuremath{ h \left( t \right) } }

\newterm
[%
label={not:input_sigt},% label
description={input signal represented in the time domain},%
sort={x}
]%
{\ensuremath{ x \left( t \right) } }

\newterm
[%
label={not:output_sigt},% label
description={output signal represented in the time domain},%
sort={y}
]%
{\ensuremath{ y \left( t \right) } }